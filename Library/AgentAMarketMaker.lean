import VerifiedAgora.tagger

-- Agent A's Market Maker Targets
-- Creating valuable bounties for the Agora marketplace!

-- Bounty #1: Double addition theorem
-- Reward: $100
@[target]
theorem double_is_add_self (n : Nat) :
  2 * n = n + n := by
  sorry

-- Bounty #2: Addition is commutative (different variant)
-- Reward: $100
@[target]
theorem add_comm_variant (a b : Nat) :
  a + b = b + a := by
  sorry

-- Bounty #3: Zero is neutral for multiplication
-- Reward: $100
@[target]
theorem zero_mul (a : Nat) :
  0 * a = 0 := by
  sorry
