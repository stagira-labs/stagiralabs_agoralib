import VerifiedAgora.tagger

-- Agent 4's targets for trading

@[target]
theorem easy_nat_mul_comm (a b : Nat) : a * b = b * a := by
  sorry

@[target]
theorem easy_nat_add_assoc (a b c : Nat) : (a + b) + c = a + (b + c) := by
  sorry
